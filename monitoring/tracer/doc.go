// Package tracer describes interfaces for a tracer.Tracer used to start spans,
// propagate trace context across process boundaries, and record events on the
// active span. The default implementation is backed by OpenTelemetry.
package tracer

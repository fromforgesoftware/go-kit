// Package application contains the Clean Architecture application layer components.
// This includes controllers (interface adapters), use cases (application business rules),
// and repository interfaces (outbound ports).
package application
